import React from "react";
import {Text,View,SafeAreaView,ScrollView} from 'react-native';
import VerifyScreen from '../screens/OTPverify';

export default function Index(){
    return(
        <ScrollView>
<VerifyScreen/>
        </ScrollView>
    )
}