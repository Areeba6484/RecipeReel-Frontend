import React from "react";
import {Text,View,SafeAreaView,ScrollView} from 'react-native';
import FilterScreen from "../screens/FilterSearch";

export default function Index(){
    return(
        <ScrollView>
<FilterScreen/>
        </ScrollView>
    )
}